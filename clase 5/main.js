 function sumar(a, b){
    return a + b;
}

// exports.add = sumar;

function restar(a, b){
    return a - b;
}

// const math= {
//     sumar(a, b){
//         return a + b;
//     }
// }

// export default {sumar, restar};

// exports.restar = restar;

const VAL = "num1";

let obj = {
    person: "cami"
}

// module.exports = sumar;
// module.exports = {sumar, restar, VAL, obj}